import { SIZES, SizeType, VARIANTS, VariantType } from '@/common/base/base.interface';

export const SIZE_LIST = Object.keys(SIZES) as SizeType[];
export const VARIANT_LIST = Object.keys(VARIANTS) as VariantType[];